import { SimplePoint } from "../../Types";
import { graphicOnCanvas } from "../data/GraphicsDataManager";

export namespace  Continuum_CollisionDetection {
    export function GetAllGraphicAroundPoint<P extends SimplePoint>(point: P, radisu: number){
 for (const g of graphicOnCanvas.values()) {
 }
    }
}
